# Xia Knowledge System

Local-first knowledge viewer built with React, TypeScript, Vite, and Mermaid.

```bash
npm run convert:raw
npm run dev
```

```bash
npm run build
```

Open `dist/index.html` directly after build.
